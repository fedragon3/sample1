# 나를 알아보는 시간 — 성격검사(TCI) + 사주팔자

### 👉 [바로 사용하기 — https://fedragon3.github.io/sample1/](https://fedragon3.github.io/sample1/)

> 위 링크는 **GitHub Pages**로 배포됩니다. 처음이라면 저장소 **Settings → Pages → Source**를
> **"Deploy from a branch"**로 두고 배포 브랜치를 선택한 뒤 저장하면 활성화됩니다.

브라우저에서 **링크만 있으면 누구나** 바로 쓰는 자가탐색 웹앱입니다. 메인 메뉴에서 네 기능을 고를 수 있습니다.

1. **기질·성격 검사** — Cloninger의 TCI 7차원 이론 기반 성격 자가탐색
2. **사주팔자·운세** — 생년월일시로 사주 8자·오행·운세를 계산 (재미·오락용)
3. **오늘의 운세 종합** — 별자리·바이오리듬·타로를 한 번에 (재미·오락용)
4. **통합 리포트** — 성격검사와 사주를 교차 해석해 시너지·보완점·인생 방향을 정리

설치·로그인·서버 없이 동작하며, 입력·응답은 어디에도 전송되지 않습니다(결과는
통합 리포트를 위해 브라우저 localStorage에만 저장).

## 통합 리포트 특징

- 성격검사(TCI 7차원)와 사주(십성·오행·대운/세운)를 **교차 해석**
- 두 검사를 모두 마치면 열리며(진행 현황 표시), 다음을 생성:
  - 한눈에 보기(타고난 결 + 발달한 성향) · 함께 강한 점(시너지) · 살펴볼 점(보완)
  - 관계와 일 · 지금의 흐름(대운·세운 + 성격 강점) · 오늘의 한마디
- 규칙 기반으로 직접 작성한 원본 텍스트. 서로 다른 전통을 함께 비추는
  자기성찰·오락용 해석이며 학술적 상관을 뜻하지 않음

## 성격검사(TCI) 특징

- **완전 정적 · 서버 불필요** — HTML/CSS/JS 순수 구현, 외부 라이브러리 0개
- **개인정보 미수집** — 모든 응답은 사용자 브라우저에서만 처리되며 어디에도 전송되지 않습니다
- **7차원 · 28하위척도 분석** — 자극추구 · 위험회피 · 사회적 민감성 · 인내력 · 자율성 · 연대감 · 자기초월
- **간단(28문항) / 심화(140문항) 두 가지 모드**
- **응답 편향 보정** — 차원별 정방향/역방향 문항을 10:10(간단 14:14)으로 균형 있게 배치해,
  한쪽으로만 응답하면 모든 점수가 50%로 수렴하도록 설계(묵종 편향 완화)
- **레이더 차트 결과** + 차원·하위척도별 해석 및 상·하위 특성 요약
- **응답 신뢰도 참고 지표** — 주의(함정) 문항, 직선응답·'보통' 과다 감지, (심화)
  상반 문항 간 내적 일관성으로 성의 없는 응답을 "양호/주의"로 표시(임상 타당성
  척도가 아닌 품질 휴리스틱)
- **문항 순서 무작위화** — 매 검사마다 순서를 섞어 순서 효과를 완화(시드 기반)
- **링크로 결과 공유** — 모드·순서 시드·응답이 URL 해시에 인코딩되어, 링크만으로
  같은 순서·같은 결과를 그대로 재현
- **라이트/다크 모드** 자동 대응, 모바일 반응형

## 사주팔자·운세 특징

- **DB 없이 계산** — 사주는 저장이 아니라 계산. 60갑자 순환 + 태양 황경(절기)으로 4기둥을 산출
  - 일주(日柱): 율리우스 적일수 기반 `(JDN + 49) mod 60` (2000-01-01=무오로 검증)
  - 연·월주: 태양 황경으로 입춘·12절 경계 판정, 오호둔 규칙
  - 시주(時柱): 일간 + 2시간 단위 지지, 오자둔 규칙
- **결과 표시** — 사주 8자(오행 색상·한자) + 오행 분포
- **운세 카테고리 선택(탭)** — 십성(十星, 오행 상생·상극) 원리로 계산
  - 📅 오늘의 운세: 오늘 일진(日辰)과 일간의 관계 + 별점·행운의 색/방위/숫자
  - 📆 올해의 운세(세운): 올해 간지를 일간과 십성으로 해석
  - 🌊 대운(10년): 월주에서 순행/역행(연간 음양+성별), 절기까지 날수÷3로 대운수 산출,
    8개 구간 간지·십성 표시, 현재 대운 자동 강조
  - 💕 연애운 · 💼 직장운 · 💰 재물운 (재성/관성/식상 기반, 성별 반영) · 🩺 건강운 · 🧭 종합·기질
  - 모두 규칙 기반으로 직접 작성한 원본 해석 텍스트
- **양력 입력**(1900~2100), 한국 표준시 시계시각 기준. 시간 모름 선택 가능
- **링크로 결과 재현** — 생년월일시가 URL 해시에 인코딩
- 재미·오락 및 자기이해용 (전문 명리 상담 대체 아님)

## 운세 통합 리포트 특징

여러 운세 시스템을 **하나로 종합**해 카테고리별로 해석합니다 (DB·서버 없이 계산).

- **입력** — 생년월일 + 태어난 시 + 성별 (사주·별자리·바이오리듬 계산용)
- **타로 뽑기(플립 애니메이션)** — 8장 뒷면 카드에서 마음 가는 4장을 순서대로 터치해 뽑음
  (오늘 → 연애 → 직장 → 재물); 메이저 아르카나 22장, 정/역방향
- **통합 리포트** — 네 시스템을 교차해 카테고리별로 해석:
  - 📅 오늘의 운세 · 💕 연애운 · 💼 직장운 · 💰 재물운 (각 판정 배지 + 조합 해석)
  - 각 카테고리에 사주(십성)·별자리·바이오리듬 신호 + **뽑은 타로 카드를 문맥 속에서 해석**
  - 상단: 종합 요약 + 근거 칩(일간·별자리·바이오리듬·세운) + 바이오리듬 그래프
- 카드 그림은 이모지, 해석은 공개 구조·아키타입 기반 원본 텍스트 (재미·오락용)
- **AI 운세 상담 챗봇(선택)** — 리포트가 나오면 그 내용을 근거로 대화형 상담이 열립니다.
  질문은 리포트를 바탕으로 답하며, 대화는 서버에 저장되지 않습니다.

### 챗봇 설정 (API 키 숨기기)

정적 사이트에는 API 키를 숨길 수 없으므로(번들·Network 탭에서 그대로 노출),
키는 **서버리스 프록시**의 서버 측 비밀로만 두고 브라우저는 프록시에만 요청합니다.

1. `proxy/README.md` 를 따라 Cloudflare Worker(무료) 를 배포하고 `ANTHROPIC_API_KEY` 를 secret 으로 등록
2. 루트 `config.js` 의 `CHAT_ENDPOINT` 에 배포된 프록시 URL 을 입력 후 커밋·배포

`CHAT_ENDPOINT` 가 비어 있으면 챗봇은 안전하게 비활성화(설정 안내)됩니다. **API 키는
`config.js`·HTML·저장소 어디에도 넣지 않습니다.**

## 구성

| 파일 | 설명 |
|------|------|
| `index.html` | 메인 메뉴 · 각 기능의 화면 구조 및 스타일 |
| `app.js` | 성격검사·사주·운세·통합 리포트 렌더 + 라우팅 |
| `questions.js` | TCI 140문항 · 7차원 · 28하위척도 정의 |
| `saju.js` | 사주 계산 엔진(절기·60갑자) 및 운세 해석 텍스트 |
| `fortune.js` | 별자리·바이오리듬·타로 계산 엔진 및 해석 텍스트 |
| `fortunereport.js` | 사주·별자리·바이오리듬·타로를 카테고리별로 종합하는 리포트 로직 |
| `integrate.js` | 성격 × 사주 통합 리포트 생성 로직 |
| `config.js` | 챗봇 프록시 URL·모델 설정 (API 키는 넣지 않음) |
| `proxy/` | 챗봇용 서버리스 프록시(Cloudflare Worker) + 배포 안내 |

## 로컬에서 실행

브라우저로 `index.html`을 여는 것만으로 동작합니다. (또는 간단한 정적 서버)

```bash
python3 -m http.server 8000
# http://localhost:8000 접속
```

## 온라인 배포 (GitHub Pages)

`master`/`main` 브랜치에 푸시하면 `.github/workflows/deploy.yml`이 자동으로 GitHub Pages에 배포합니다.
저장소 **Settings → Pages → Source** 를 **GitHub Actions** 로 설정하면 됩니다.
배포 후 발급되는 링크를 공유하면 누구나 접속할 수 있습니다.

## ⚠️ 안내

본 검사는 교육 및 자기이해를 위한 **비공식 자가탐색 도구**입니다.
정식 TCI 검사의 저작권 문항이 아니라, 공개된 7차원 이론을 바탕으로 **독자적으로 작성한 문항**을 사용합니다.
심리 상담이나 임상적 진단을 대체하지 않습니다.
